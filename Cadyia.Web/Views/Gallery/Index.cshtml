@model  IEnumerable<Cadyia.Data.Entities.Plan>
@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>
<ul>
    @foreach (var plan in Model)
    {
        <li>
            <a asp-controller="Gallery" asp-action="Detail" asp-route-id="@plan.PlanId">@plan.Title</a>
        </li>
    }
</ul>


